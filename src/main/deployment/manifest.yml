---
applications:
- name: ap-appmonitor
  memory: 1024M
  path: ../../../build/libs/appmonitor-0.0.1-SNAPSHOT.jar
  timeout: 60
  services:
  - p-config-server
  - p-service-registry
  buildpack: java_buildpack
  env:
    SPRING_PROFILES_ACTIVE: cloud
